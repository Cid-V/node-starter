{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program (Windows)",
            "program": "${workspaceFolder}\\app.js"
        },
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program (MacOS)",
            "program": "${workspaceFolder}/app.js"
        }
    ]
}
